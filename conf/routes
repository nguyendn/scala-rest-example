# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET      /transactionservice/types/:types             controllers.Application.listTypes(types: String)
GET      /transactionservice/sum/:id             controllers.Application.listTypes(id: Long)
PUT     /transactionservice/:id                       controllers.Application.saveTransaction(id: Long)
